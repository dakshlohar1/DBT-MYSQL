wisflux:
  target: dev
  outputs:
    dev:
      type: mysql
      server: localhost
      port: 3306
      schema: "airport_db"
      username: "{{ env_var('DB_USER') }}"
      password: "{{ env_var('DB_PASSWORD') }}"
      ssl_disabled: True